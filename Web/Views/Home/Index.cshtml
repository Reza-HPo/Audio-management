@model Web.Models.ViewModels.Home.HomeViewModel

@{
    ViewData["Title"] = "خانه";
}

@section Styles {
    <link rel="stylesheet" href="~/css/mainpage.css" />
    <link rel="stylesheet" href="~/css/player.css" />
}

<main class="home-page">

    <!-- =========================================================
     HERO
     ========================================================= -->

    <section class="home-hero">

        <div class="container">

            <div class="home-hero-content">

                <!-- Text -->

                <div class="home-hero-text">

                    @if (!string.IsNullOrWhiteSpace(Model.Content.HeroSubtitle))
                    {
                        <span class="home-hero-eyebrow">
                            @Model.Content.HeroSubtitle
                        </span>
                    }


                    @if (!string.IsNullOrWhiteSpace(Model.Content.HeroTitle))
                    {
                        <h1 class="home-hero-title">
                            @Model.Content.HeroTitle
                        </h1>
                    }


                    @if (!string.IsNullOrWhiteSpace(Model.Content.HeroDescription))
                    {
                        <p class="home-hero-description">
                            @Model.Content.HeroDescription
                        </p>
                    }


                    @if (!string.IsNullOrWhiteSpace(Model.Content.HeroButtonText))
                    {
                        <div class="home-hero-actions">

                            <a href="@(string.IsNullOrWhiteSpace(Model.Content.HeroButtonUrl)
                                                                     ? "/Archive"
                                                                     : Model.Content.HeroButtonUrl)"
                               class="home-hero-button">

                                <span>
                                    @Model.Content.HeroButtonText
                                </span>

                                <span class="home-hero-button-icon">
                                    ←
                                </span>

                            </a>

                        </div>
                    }

                </div>


                <!-- Image -->
                @if (!string.IsNullOrWhiteSpace(Model.Content.HeroImageUrl))
                {
                    <div class="home-hero-visual">

                        <div class="home-hero-image-wrapper">

                            <img src="@Model.Content.HeroImageUrl"
                                 alt="مکتب اهواز"
                                 class="home-hero-image"
                                 loading="eager" />

                        </div>

                    </div>
                }

            </div>

        </div>

    </section>


    <!-- =====================================================
         STATISTICS
         ===================================================== -->

    <section class="home-stats">

        <div class="container">

            <div class="home-stats-grid">

                <!-- Audio -->

                <div class="home-stat-card">

                    <div class="home-stat-icon">
                        ♪
                    </div>

                    <div class="home-stat-content">

                        <strong>
                            @Model.AudioCount
                        </strong>

                        <span>
                            فایل صوتی
                        </span>

                    </div>

                </div>


                <!-- Speakers -->

                <div class="home-stat-card">

                    <div class="home-stat-icon">
                        ◉
                    </div>

                    <div class="home-stat-content">

                        <strong>
                            @Model.SpeakerCount
                        </strong>

                        <span>
                            سخنران
                        </span>

                    </div>

                </div>


                <!-- Categories -->

                <div class="home-stat-card">

                    <div class="home-stat-icon">
                        ◈
                    </div>

                    <div class="home-stat-content">

                        <strong>
                            @Model.CategoryCount
                        </strong>

                        <span>
                            دسته‌بندی
                        </span>

                    </div>

                </div>

            </div>

        </div>

    </section>


    <!-- =====================================================
         LATEST AUDIOS
         ===================================================== -->

    <section class="home-section"
             id="latest-audios">

        <div class="container">

            <!-- Section Header -->

            <div class="home-section-header">

                <div>

                    <span class="home-section-eyebrow">
                        تازه‌ترین محتوا
                    </span>

                    <h2>
                        آخرین سخنرانی‌ها
                    </h2>

                    <p>
                        جدیدترین فایل‌های صوتی منتشر شده در مکتب اهواز
                    </p>

                </div>

                <a asp-controller="Archive"
                   asp-action="Index"
                   class="home-section-link">

                    مشاهده همه

                    <span>
                        ←
                    </span>

                </a>

            </div>


            <!-- Audio Grid -->

            @if (Model.LatestAudios.Any())
            {
                <div class="home-audio-grid">

                    @foreach (var audio in Model.LatestAudios)
                    {
                        <article class="home-audio-card">


                            <!-- Card Header -->

                            <div class="home-audio-card-header">


                                <!-- Cover -->

                                <div class="home-audio-cover">

                                    @if (!string.IsNullOrWhiteSpace(audio.CoverImageUrl))
                                    {
                                        <img src="@audio.CoverImageUrl"
                                             alt="@audio.Title"
                                             loading="lazy" />
                                    }
                                    else
                                    {
                                        <div class="home-audio-cover-placeholder">
                                            ♪
                                        </div>
                                    }

                                </div>


                                <!-- Info -->

                                <div class="home-audio-info">

                                    @if (audio.Categories.Any())
                                    {
                                        <div class="home-audio-category">

                                            @string.Join(" • ", audio.Categories)

                                        </div>
                                    }

                                    <h3>

                                        <a asp-controller="Archive"
                                           asp-action="Details"
                                           asp-route-id="@audio.Id">

                                            @audio.Title

                                        </a>

                                    </h3>


                                    @if (!string.IsNullOrWhiteSpace(audio.SpeakerName))
                                    {
                                        <span class="home-audio-speaker">

                                            @audio.SpeakerName

                                        </span>
                                    }

                                </div>

                            </div>


                            <!-- Meta -->

                            <div class="home-audio-meta">

                                @if (audio.PublishedAt.HasValue)
                                {
                                    <span>

                                        @audio.PublishedAt.Value.ToLocalTime().ToString("yyyy/MM/dd")

                                    </span>
                                }


                                @if (audio.Duration.HasValue)
                                {
                                    <span>

                                        @audio.Duration.Value.ToString(@"mm\:ss")

                                    </span>
                                }

                            </div>


                            <!-- Player -->

                            <div class="custom-audio-player"
                                 data-audio-player>

                                @if (!string.IsNullOrWhiteSpace(audio.FileName))
                                {
                                    var audioUrl = audio.FileName;

                                    if (!audioUrl.StartsWith("/"))
                                    {
                                        audioUrl = "/" + audioUrl;
                                    }

                                    if (!audioUrl.StartsWith(
                                        "/uploads/",
                                        StringComparison.OrdinalIgnoreCase))
                                    {
                                        audioUrl =
                                            "/uploads/audio/" +
                                            audioUrl.TrimStart('/');
                                    }

                                    <audio class="audio-player"
                                           preload="metadata"
                                           src="@audioUrl">
                                    </audio>
                                }


                                <div class="player-top">

                                    <div class="player-cover">

                                        @if (!string.IsNullOrWhiteSpace(audio.CoverImageUrl))
                                        {
                                            <img src="@audio.CoverImageUrl"
                                                 alt="@audio.Title"
                                                 loading="lazy" />
                                        }
                                        else
                                        {
                                            <div class="player-cover-placeholder">
                                                ♪
                                            </div>
                                        }

                                    </div>


                                    <div class="player-info">

                                        <span class="player-label">
                                            پخش صوت
                                        </span>

                                        <span class="player-title">
                                            @audio.Title
                                        </span>

                                    </div>


                                    <button type="button"
                                            class="player-play-button"
                                            aria-label="پخش @audio.Title">

                                        <span class="play-icon">
                                            ▶
                                        </span>

                                        <span class="pause-icon">
                                            ❚❚
                                        </span>

                                    </button>

                                </div>


                                <div class="player-progress-area">

                                    <span class="player-time current-time">
                                        00:00
                                    </span>

                                    <div class="player-progress">

                                        <input type="range"
                                               class="progress-range"
                                               min="0"
                                               max="100"
                                               value="0"
                                               step="0.1"
                                               aria-label="موقعیت پخش" />

                                    </div>

                                    <span class="player-time total-time">

                                        @if (audio.Duration.HasValue)
                                        {
                                            @audio.Duration.Value.ToString(@"mm\:ss")
                                        }
                                        else
                                        {
                                            <text>00:00</text>
                                        }

                                    </span>

                                </div>


                                <div class="player-bottom">

                                    <div class="player-volume">

                                        <button type="button"
                                                class="player-control volume-button"
                                                aria-label="قطع صدا">

                                            🔊

                                        </button>

                                        <input type="range"
                                               class="volume-range"
                                               min="0"
                                               max="1"
                                               step="0.01"
                                               value="1"
                                               aria-label="میزان صدا" />

                                    </div>


                                    <a asp-controller="Archive"
                                       asp-action="Details"
                                       asp-route-id="@audio.Id"
                                       class="player-more">

                                        جزئیات

                                    </a>

                                </div>

                            </div>

                        </article>
                    }

                </div>
            }
            else
            {
                <div class="home-empty">

                    <div class="home-empty-icon">
                        ♪
                    </div>

                    <h3>
                        هنوز سخنرانی‌ای منتشر نشده است
                    </h3>

                    <p>
                        به‌زودی محتوای صوتی جدید در این بخش قرار خواهد گرفت.
                    </p>

                </div>
            }

        </div>

    </section>


    <!-- =====================================================
         CATEGORIES
         ===================================================== -->

    <section class="home-section home-categories-section">

        <div class="container">

            <div class="home-section-header">

                <div>

                    <span class="home-section-eyebrow">
                        موضوعات
                    </span>

                    <h2>
                        دسته‌بندی‌های آرشیو
                    </h2>

                    <p>
                        محتوای مورد نظر خود را بر اساس موضوع پیدا کنید.
                    </p>

                </div>

                <a asp-controller="Archive"
                   asp-action="Index"
                   class="home-section-link">

                    آرشیو کامل

                    <span>
                        ←
                    </span>

                </a>

            </div>


            @if (Model.Categories.Any())
            {
                <div class="home-category-grid">

                    @foreach (var category in Model.Categories)
                    {
                        <a asp-controller="Archive"
                           asp-action="Index"
                           asp-route-categoryId="@category.Id"
                           class="home-category-card">

                            <div class="home-category-icon">
                                ◈
                            </div>

                            <div class="home-category-content">

                                <h3>
                                    @category.Name
                                </h3>

                                <span>
                                    @category.AudioCount فایل صوتی
                                </span>

                            </div>

                            <span class="home-category-arrow">
                                ←
                            </span>

                        </a>
                    }

                </div>
            }
            else
            {
                <div class="home-empty">

                    <div class="home-empty-icon">
                        ◈
                    </div>

                    <h3>
                        دسته‌بندی‌ای وجود ندارد
                    </h3>

                </div>
            }

        </div>

    </section>


    <!-- =====================================================
         SPEAKERS
         ===================================================== -->

    <section class="home-section home-speakers-section">

        <div class="container">

            <div class="home-section-header">

                <div>

                    <span class="home-section-eyebrow">
                        سخنرانان
                    </span>

                    <h2>
                        سخنرانان مکتب
                    </h2>

                    <p>
                        سخنرانی‌های ارائه‌شده توسط اساتید و سخنرانان
                    </p>

                </div>

                <a asp-controller="Archive"
                   asp-action="Index"
                   class="home-section-link">

                    مشاهده آرشیو

                    <span>
                        ←
                    </span>

                </a>

            </div>


            @if (Model.Speakers.Any())
            {
                <div class="home-speaker-grid">

                    @foreach (var speaker in Model.Speakers)
                    {
                        <a asp-controller="Archive"
                           asp-action="Index"
                           asp-route-speakerId="@speaker.Id"
                           class="home-speaker-card">


                            <!-- Image -->

                            <div class="home-speaker-image">

                                @if (!string.IsNullOrWhiteSpace(speaker.ImageUrl))
                                {
                                    <img src="@speaker.ImageUrl"
                                         alt="@speaker.Name"
                                         loading="lazy" />
                                }
                                else
                                {
                                    <div class="home-speaker-placeholder">
                                        ◉
                                    </div>
                                }

                            </div>


                            <!-- Info -->

                            <div class="home-speaker-info">

                                <h3>
                                    @speaker.Name
                                </h3>

                                <span>
                                    @speaker.AudioCount سخنرانی
                                </span>

                            </div>


                            <span class="home-speaker-arrow">
                                ←
                            </span>

                        </a>
                    }

                </div>
            }
            else
            {
                <div class="home-empty">

                    <div class="home-empty-icon">
                        ◉
                    </div>

                    <h3>
                        هنوز سخنرانی ثبت نشده است
                    </h3>

                </div>
            }

        </div>

    </section>


    <!-- =====================================================
         FINAL CTA
         ===================================================== -->

    <section class="home-cta">

        <div class="container">

            <div class="home-cta-content">

                <span class="home-cta-eyebrow">
                    آرشیو صوتی مکتب اهواز
                </span>

                <h2>
                    به دنبال یک سخنرانی خاص هستید؟
                </h2>

                <p>
                    در آرشیو صوتی جستجو کنید و محتوای مورد نظر خود را
                    سریع و آسان پیدا کنید.
                </p>

                <a asp-controller="Archive"
                   asp-action="Index"
                   class="home-btn home-btn-primary">

                    ورود به آرشیو

                </a>

            </div>

        </div>

    </section>

</main>


@section Scripts {

    <script src="~/js/archive-player.js"></script>

}